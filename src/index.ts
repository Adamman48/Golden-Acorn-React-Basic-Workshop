'use strict';

import React from 'react';
import ReactDOM from 'react-dom';

const Button = (inputString: string, inputFunc: CallableFunction) => {
  const element: JSXElement = (
    <div>
      <h1>Hello, world!</h1>
    </div>
  )
  ReactDOM.render(

  )
};

const Display = (inputNum: number) => {

};

class GoldenAcornApp extends React.Component {
  state: number;
  
  constructor(props) {
    super(props);
    this.state = 0;
  };

  render() {
    return (
      Button
    );
  }
}